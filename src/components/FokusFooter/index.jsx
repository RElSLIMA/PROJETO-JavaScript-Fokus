import { styles } from "@/styles/index/styles";
import { Text, View } from "react-native";

export const FokusFooter = () => {
  return (
    <View style={styles.footer}>
      <Text style={styles.footerText}>Desenvolvido por Gabriel Reis</Text>
    </View>
  );
};
